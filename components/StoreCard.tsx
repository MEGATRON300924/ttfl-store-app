import { Ionicons } from "@expo/vector-icons"; import { Image,Pressable,StyleSheet,Text,View } from "react-native"; import { router } from "expo-router"; import { GlassCard } from "@/components/GlassCard"; import { theme } from "@/lib/theme"; export type MobileStore={id:string;name:string;slug:string;logoUrl?:string|null;location?:string|null;rating?:number;productCount?:number;verified?:boolean}; export function StoreCard({store}:{store:MobileStore}){return <Pressable onPress={()=>router.push({pathname:"/vendor/[slug]",params:{slug:store.slug}})} style={({pressed})=>[styles.wrap,pressed&&styles.pressed]}><GlassCard style={styles.card}><View style={styles.logo}>{store.logoUrl?<Image source={{uri:store.logoUrl}} style={styles.logoImage} resizeMode="cover"/>:<Ionicons name="storefront-outline" size={24} color={theme.colors.graphite600}/>}</View><View style={styles.copy}><View style={styles.nameRow}><Text numberOfLines={1} style={styles.name}>{store.name}</Text>{store.verified&&<Ionicons name="checkmark-circle" size={15} color={theme.colors.green600}/>}</View><Text style={styles.meta}>{store.location||"Nigeria"} · {store.productCount??0} products</Text>{!!store.rating&&<View style={styles.rating}><Ionicons name="star" size={12} color={theme.colors.gold600}/><Text style={styles.meta}>{Number(store.rating).toFixed(1)}</Text></View>}</View><Ionicons name="chevron-forward" size={17} color={theme.colors.graphite300}/></GlassCard></Pressable>} const styles=StyleSheet.create({wrap:{width:"100%"},card:{padding:13,borderRadius:16,flexDirection:"row",alignItems:"center"},pressed:{opacity:.82},logo:{width:50,height:50,borderRadius:12,overflow:"hidden",backgroundColor:theme.colors.cloud100,alignItems:"center",justifyContent:"center"},logoImage:{width:"100%",height:"100%"},copy:{flex:1,marginLeft:12},nameRow:{flexDirection:"row",alignItems:"center",gap:5},name:{fontSize:14,fontWeight:"800",color:theme.colors.graphite950,flexShrink:1},meta:{fontSize:11,color:theme.colors.graphite600,marginTop:4},rating:{flexDirection:"row",alignItems:"center",gap:4,marginTop:3}});